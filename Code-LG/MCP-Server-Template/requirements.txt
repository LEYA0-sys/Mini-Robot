# MCP 服务器依赖文件
# requirements.txt

# 基础 MCP 依赖
mcp>=1.0.0
pydantic>=2.0.0

# SSE (Server-Sent Events) 方式依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# HTTP 流式方式依赖
flask>=2.3.0
flask-cors>=4.0.0

# 可选依赖 (用于增强功能)
websockets>=11.0.0  # WebSocket 支持
requests>=2.31.0    # HTTP 客户端 (用于测试)
psutil>=5.9.0       # 系统监控

# 开发依赖 (可选)
# pytest>=7.4.0        # 测试框架
# black>=23.0.0         # 代码格式化
# flake8>=6.0.0         # 代码检查
